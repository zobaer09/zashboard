% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_io.R
\name{zashboard_validate_spec}
\alias{zashboard_validate_spec}
\title{Validate a Zashboard spec (light V1 checks)}
\usage{
zashboard_validate_spec(spec)

zashboard_validate_spec(spec)
}
\arguments{
\item{spec}{A list-like spec.}
}
\value{
Invisibly returns \code{spec} if validation passes; otherwise throws an error.

Invisibly returns \code{spec} if valid; otherwise throws an error.
}
\description{
Validates the minimal V1 shape aligned with the starter template:
\itemize{
\item top-level \code{datasets} (named list)
\item optional \code{filters} (list)
\item \code{charts} (list of chart configs, each with \code{id} and \code{type})
\item \code{layout} (list)
}
}
\details{
This is intentionally light-weight for V1. Stricter schema checks can be added later.
}
