% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{zashboard_init}
\alias{zashboard_init}
\title{Initialize a new Zashboard spec folder}
\usage{
zashboard_init(path, overwrite = FALSE)
}
\arguments{
\item{path}{Directory to create/populate.}

\item{overwrite}{Logical; if \code{TRUE}, allows writing into a non-empty dir. Default \code{FALSE}.}
}
\value{
Invisibly returns the normalized target directory path.
}
\description{
Copies the packaged template \code{zashboard.yml} into a new directory and
writes a minimal \code{manifest.json} derived from it. Nothing destructive:
the target directory must be empty unless \code{overwrite = TRUE}.
}
\examples{
\donttest{
dir_out <- zashboard_init(file.path(tempdir(), "my-zashboard"), overwrite = TRUE)
list.files(dir_out)
}
}
